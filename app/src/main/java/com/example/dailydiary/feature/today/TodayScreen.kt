package com.example.dailydiary.feature.today

import androidx.compose.runtime.Composable
import com.example.dailydiary.feature.common.EmptyFeatureScreen

@Composable
fun TodayScreen() {
    EmptyFeatureScreen(
        title = "今日记录",
        subtitle = "这里将用于记录当天心情和日记。"
    )
}
